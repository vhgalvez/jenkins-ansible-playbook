# templates/jenkins/values.yaml.j2
controller:
  serviceType: ClusterIP
  servicePort: 8080
  jenkinsOpts: ""  # No usar --prefix=/ para evitar 404

  ingress:
    enabled: true
    ingressClassName: traefik
    hostName: "jenkins.local"
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
      traefik.ingress.kubernetes.io/router.tls: "true"
      traefik.ingress.kubernetes.io/auth-type: basic
      traefik.ingress.kubernetes.io/auth-secret: jenkins-auth-secret
    paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: jenkins
            port:
              number: 8080
    tls:
      - hosts:
          - "jenkins.local"
        # secretName: jenkins-tls  # Descomenta si tienes un certificado personalizado
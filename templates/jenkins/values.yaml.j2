# roles\jenkins\templates\values.yaml.j2
controller:
  ingress:
    enabled: true
    hostName: "{{ jenkins_ingress_host }}"
    annotations:
      kubernetes.io/ingress.class: traefik
    tls:
      - hosts:
          - "{{ jenkins_ingress_host }}"

  JCasC:
    enabled: false  # 🚀 Desactiva Configuration as Code (esto soluciona tu error de InitContainer)

  installPlugins: []  # 🚀 También evita que busque plugins al inicio si no tienes lista una lista de plugins

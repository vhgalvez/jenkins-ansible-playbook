# roles/jenkins/templates/values.yaml.j2
controller:
  serviceType: ClusterIP
  servicePort: 8080  # ✅ Necesario para que el Ingress funcione
  jenkinsOpts: "--prefix=/"  # ✅ Fuerza a Jenkins a servir en /

  ingress:
    enabled: true
    hostName: "{{ jenkins_ingress_host }}"
    ingressClassName: traefik
    annotations:
      kubernetes.io/ingress.class: traefik
      traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
      traefik.ingress.kubernetes.io/router.tls: "true"
      traefik.ingress.kubernetes.io/redirect-entry-point: websecure
    path: /
    pathType: Prefix
    tls:
      - hosts:
          - "{{ jenkins_ingress_host }}"
        # secretName: jenkins-tls  # coméntalo si no usas certificado

  JCasC:
    enabled: false

  installPlugins: []